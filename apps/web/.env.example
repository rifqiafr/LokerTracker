# URL Backend API (Opsional: Jika backend dideploy di Render/Railway)
# Jika tidak diisi, frontend akan menggunakan default http://localhost:5000/api atau fallback ke mode lokal (LocalStorage)
VITE_API_BASE_URL=https://your-api-domain.onrender.com/api
